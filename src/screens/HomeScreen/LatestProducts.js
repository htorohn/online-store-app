import React, { Component } from 'react'
import { connect } from 'react-redux'

class LatestProducts extends Components {
    render(){
        return (
            null
        )
    }
}

export default connect(null,null)(LatestProducts)